#include<stdio.h>
#include<conio.h>
mrge(arr,lb,mid,ub)
{
	i=ib;
	j=mid+1;
	k=lb;
	while(i<=mid && j<=ub)
	if(a[])
}
void merge(arr,lb,ub)
{
	if(lb<ub)
	{
		
		mid=(lb+ub)/2;
		}
		merge(arr,lb,mid);
		merge(arr,mid+1,ub);
		merging(arr,lb,mid,ub);
}
